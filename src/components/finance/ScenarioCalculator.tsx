export function ScenarioCalculator({ title, description }: { title: string; description: string }) {
  return (
    <section className="placeholder-panel" data-calculator-status="notImplemented">
      <h2>{title}</h2>
      <p>{description}</p>
    </section>
  );
}
