export function SanatoriumMap({ title, description }: { title: string; description: string }) {
  return (
    <section className="placeholder-panel">
      <h2>{title}</h2>
      <p>{description}</p>
    </section>
  );
}
